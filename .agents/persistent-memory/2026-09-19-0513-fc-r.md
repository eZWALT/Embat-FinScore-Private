# 2026-09-19-0513 — drop contemporaneous f_fc_r from the 44

- **Author:** agent (Cursor)
- **When:** 2026-09-19 05:13 CEST

## What changed

[f_fc_r leftover QA](572fb928-40f3-4948-9ab9-0fde761a9d50):
**DROP** contemporaneous `f_fc_r` from the 44 as unused leftover, not
a twin of `f_ds_r` (ρ **0.205**, R² **0.005**). Y3 leftover after
`f_ds_r` rank **0.464** / after days **0.449** (OLS 0.648 is a fake
days leak). Single **0.559** loses to size 0.617 / days 0.711.
**KEEP** `f_fc_r_lag3` on TURNOVER (do not rip; TURNFC0/1 failed fold
4). Y7 leftover after issued_lag1 **0.563** / TURN_NOFC **0.557** —
CLOSE as a TURNOVER add-on. Not the binary Y9 (ρ **0.132**, Jaccard
**0.113**). Q6 **CLOSE** (lag3 empty until so-far≥6). Dark 470 have
fc (access ≠ ERP). Store flow stays. Night quotes unchanged.

## Decisions

- Resume same Family F owner for unused `f_ds_r` leftover after days
  (15-col card stem; still on the 44). Cap 3.
- Still running: a_n_tx, DSO.

## Still unknown

Whether `e_dso_proxy` leftover after days is a >24 tail. Whether
`a_n_tx` is a days twin on the card. Whether `f_ds_r` leftover after
days lives.
