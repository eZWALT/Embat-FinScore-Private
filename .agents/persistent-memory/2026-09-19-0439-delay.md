# 2026-09-19-0439 — delay leftover KEEP; DROP from the 44 as Y3 X

- **Author:** agent (Cursor)
- **When:** 2026-09-19 04:39 CEST

## What changed

[Delay / overdue QA](47815ca4-ee6d-4a51-b38e-8fdba454f445):
**KEEP** `e_delay_coll` as a Y7 leftover after DSO (**0.581**) and
`issued_lag1` (**0.583**; both **0.584**). Not a twin (ρ vs DSO **0.214**).
Leftover ≈ same-n raw — residualizing does not create a new object. ICC
**0.922**, demean **0.521** (who-pays-late style). **CLOSE** as a TURNOVER
add-on: do not change **0.720 / 0.712**. **CLOSE** `e_delay_paid` /
`e_ar_overdue` as Y7 leftover (0.449 / 0.547 after DSO). **CLOSE / DROP
from the 44** as Y3 X (**0.512**; leftover after days **0.427** vs days
**0.711**). **CLOSE** Q6 (empty until month 7). **CLOSE** 3m-window leftover
(**0.509**). **PARK** as a health Y. Javier delay **ρ=1 SAME**; overdue
3m SAME / all-open CLOSE. Fold 4 **0.680** is issued, not delay. Dark 470
stay NaN.

## Decisions

- Do not invent `y_delay`. Off the 15-col card. Do not grow TURNOVER.
- Do not resume delay. Seat: unused `e_dpo_proxy`. Cap 3.
- Still running: pending, g_has_rest.

## Still unknown

Whether pending amount-share leftover after DSO lives. Whether remaining
`g_has_*` leave the 44.
