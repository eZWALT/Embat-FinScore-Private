# 2026-09-19 1515 — Health Sentinel agent API (Ask + Watcher reply)

- **Author:** Cursor Grok
- **When:** 2026-09-19 15:15 CEST

## What changed

Agent runtime in `product/web/` (Next.js). `poc/` untouched.

- Deps: `ai` 7, `@ai-sdk/openai`, `@ai-sdk/react`, `zod`. Helmcode via `createOpenAI` (`HELMCODE_BASE_URL` default `https://api.helmcode.com/v1`, `HELMCODE_API_KEY`, `POC_LLM_MODEL` default `deepseek-v4-flash`). Documented in `product/web/.env.example`.
- Tools in `src/lib/agent/tools.ts` read `createScoreRepository()` (Neon). Control / cluster / forecast extras query `analytics.*` on the current run (repository `listGroups().control` is always `null`).
- `query_clean_db`: `checkSql` then rewrite `clean.*` → `core.*`. No DuckDB. Empty `core.companies` → `{error:"records not mounted"}`.
- `POST /api/ask` (`loadSystemPrompt("chat")`) and `POST /api/watcher/reply` (`loadSystemPrompt("sentinel")`). `streamText` + tool loop (`isStepCount(8)`). UI message stream includes tool-call names for `ToolCallChip`. Opening Watcher month posts are not generated here.
- Ask and Watcher reply UIs (`ask-chat.tsx`, `watcher-reply.tsx`, `agent-chat.tsx`) stream from the new routes and render `ToolCallChip`. Opening Watcher posts stay deterministic.
- `tsc --noEmit` passes.

## Decisions

- Prompts stay in markdown; routes pass only `session` `company_id` / `group_id` / `as_of` as `loadSystemPrompt` extra.
- `plot_series` returns a spec (`plot`) for the UI; the thin chat only shows the tool chip, not a chart.
- `sql-guard` accepts `core.*` as well as `clean.*`. `banking_products` / `debt_products` rewrite to `core.products`; `debt_schedule_config` → `core.debt_schedule`.

## Still unknown

- `HELMCODE_API_KEY` on the Vercel project.
- When `core` facts (transactions / invoices) get loaded (Free storage cap). Until then record questions stay `{error:"records not mounted"}`.
- Column names differ on some `core` tables (`transactions.booked_at` vs prompt `date`; `balances.as_of_date`). Unmapped until facts are mounted.
