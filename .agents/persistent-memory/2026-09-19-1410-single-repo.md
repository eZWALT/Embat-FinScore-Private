# 2026-09-19-1410 — one repo: the public sibling is retired

- **Author:** Walter (with Cursor agent)
- **When:** 2026-09-19 ~14:10 CEST

## Decision (Walter)

`eZWALT/Embat-FinScore-Private` is the only repo. It will be flipped to public as is at the end of the hackathon. The public sibling `eZWALT/Embat-FinScore` is being deleted by Walter; nothing is synced to it any more. Vercel must point at this repo (Root Directory `product/web`, as before).

## Checked before retiring it

- Every tracked file in the public repo (at `0b9b7e0`) also exists here; nothing is lost by deleting it. The two syncs earlier today (`ad218b8`, `0b9b7e0`) were copies of this tree.
- Differences that stay here only: this repo's `LICENSE` is MIT (public had Apache 2.0), and this README is the working one. Decide the licence before flipping to public.
- The raw CSVs (`data/*.csv`, 647 MB) are **tracked** in this repo by accident of history. Before making it public, either keep them (organizers' synthetic data; check the zip's terms) or `git rm --cached` them and rewrite history; `.gitignore` already excludes `data/**/*.csv` for new files.

## Changed

- `AGENTS.md`, `README.md`, `data/README.md`: sibling references removed.
- The `poc/README`, `DATA_CONTRACT.md` and code never referenced the public repo.

## Still unknown

- Licence for the public flip (MIT vs Apache 2.0).
- Whether to purge the CSVs from history before the flip.
