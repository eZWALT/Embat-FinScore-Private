# 2026-09-19-0213 — Y9 why: not outflow, not usable mix

- **Author:** agent `0511f2af` (night child)
- **When:** 2026-09-19 02:13 CEST

## What changed

`analysis/evaluate/y9_why.py` + `analysis/outputs/y9_why.md` +
`y9_fee_share_quintiles.png`. Family M built in memory. No parquet merge.
No GBM. No `build_targets`. Registry appended once (143 train-only rows).

## Decisions

- Y9 is **not an outflow tail** (31% of own-p80 pos are high `a_out6`;
  2×2 neither 42%).
- Mix columns that beat `a_out6` 0.565 (`m_fee_share` 0.613, `m_fin_share`
  0.618, `m_fee_n_share` 0.611) are **fee-presence** rewrites of
  `a_fin_cost>0` (any-fin CV 0.610, gap +0.001). PARK. Merge **no**.
- `m_int_share` 0.525 CLOSE (loses to outflow). Fee vs int ρ=+0.11,
  not substitutes.
- Lag-1 fee-share 0.571 does not clear 0.565+0.02. Not Q6. t3 already CLOSE.
- Dark 470 vs invoiced 744 Y9 bases identical (14.1% / 19.1%). Never D/E.
- Spike is turning (company ρ −0.03). Label stays. GBM PARK explained.

## Still unknown

110 mixed-dark spike residual after no-fin (21.4% vs 12.9%) — not a
merge reason; do not put D/E on it.
