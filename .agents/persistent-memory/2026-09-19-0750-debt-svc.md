# 2026-09-19-0750 — a_debt_service leftover CLOSE

- **Author:** agent 572fb928
- **When:** 2026-09-19 07:50 CEST

## What changed

Unused leftover of contemporaneous `a_debt_service` after
`c_n_days_with_tx` as Y3 X.
Owned: `analysis/evaluate/debt_svc_qa.py`, `analysis/outputs/debt_svc_qa.md`,
`analysis/outputs/debt_svc_qa.png`, append-only registry,
`overnight/waves/wave4_debt_svc.md`.

`a_debt_service` = -sum(amount | grp = debt_service). Exact identity
with `f_debt_service` (max abs diff 0, ρ=1.000). Twin of `f_ds_r` ρ=0.881.

## Decisions

- **CLOSE unused leftover** / **DROP from the 44**. Honest leftover
  after days rank **0.484 dies** (OLS 0.631 fake=False, almost=True,
  ρ(resid,days)=−0.742). KEEP-as-X fails: leftover <0.55, **beat-size
  FAIL** 0.613 vs 0.617 (Δ=−0.004), **TWIN** of `f_ds_r` / `f_debt_service`.
  NOT SIZE (ρ vs log1p(a_in3)=0.322). Inverse days leftover 0.673 lives.
- Off the 15-col card. **PARK `y_debt_service`** — leftover did not
  clear KEEP-as-X. Do not grow TURNOVER 0.720.
- `f_ds_r` leftover after days **0.528 CONFIRM DROP**. `f_fc_r`
  leftover **0.449 CONFIRM DROP**. KEEP `f_fc_r_lag3` on TURNOVER.
  Store flow stays. Javier `debt_serv_r` SAME.
- 76.4% zeros. Bootstrap leftover p05/p50/p95 0.447 / 0.514 / 0.548
  share<0.55=95%. ICC 0.755 WITHIN / month shock, acf1 0.026.
- Demean leftover 0.544 dies. Q6 lag1 leftover 0.522 dies. Dark 0.474
  / ERP 0.454 die. leftover after days+size 0.487 dies. leftover after
  days+f_ds_r 0.477 dies. leftover after a_n_tx 0.485 dies.
- Slice leftovers that live (T3 0.610, Q4-days 0.642, ds>0 0.640) are
  leftover of **`f_ds_r` on large/busy firms** (T3 f_ds_r leftover
  0.634 Y3 0.660; Q4 f_ds_r leftover 0.671 Y3 0.674). Do **not**
  reopen `f_ds_r` leftover 0.528 DROP. MoM leftover 0.600 dies after
  size (0.528). never-ds leftover 0.667 is leftover of days among
  all-zero companies (ρ=−0.742).
- Night quotes unchanged: Y3 0.762/0.752, days 0.711, size 0.617,
  TURNOVER 0.720/0.712.

## Still unknown

None for `a_debt_service` KEEP-as-X. T3/Q4 leftover of `f_ds_r` is a
slice of an already-DROP feature — not this ticket.
