# 2026-09-19-1048 — v0 dummy FICO-like score

- **Author:** agent (Aura), after the cash-columns / deterioration question
- **When:** 2026-09-19 ~10:48 CEST
- **Repo:** Embat-FinScore-Private

## What changed

First product number. Code in `product/score/`. Run:
`PYTHONPATH=. python -m product.score`.

Card is a priori. Only fit = train-only empirical percentiles (holdout
out). New credit empty → its 10 FICO points go to amounts owed.

| Category | Weight | Items |
|---|---:|---|
| Payment history | 35 | `e_ar_overdue_30`, `e_ap_overdue_30`, `e_delay_coll`, `e_delay_paid` |
| Amounts owed | 40 | `b_runway`, `f_ds_r`, `f_fc_r` |
| Length / stability | 15 | `active_share_6` |
| Mix | 10 | `d_cust_top1` tail only (0.70 → 100, 0.975 → 0) |

Going dark: no movements this month cap 50; recency > 45d cap 35. Never
raises the score. Trail length is a thin-file flag, not points (connection
artefact). SS / payroll / days are not on the card (Y3 trap).

Stretch `clip(50 + 2*(raw−50), 0, 100)` after the weighted mean so the
scale can reach the brief's 80s. Not a fit.

## First train snapshot (latest month)

p10 / p50 / p90 of `score_3m` = 17 / 47 / 80. 39% no invoices. 10% going
dark. Tests in `product/tests/test_dummy_score.py` (3).

## Decisions

- Simple dummy the web can bind to; not validated on the eight accepted Ys yet.
- Customer-loss stays an alert, not a score input.
- No-invoice companies are reweighted (plan); the UI must show the
  confidence flag or they look like 90s on cash alone.

## Still unknown

- AUROC of this card vs `y2_neg_2of3`, `y4_*`, `y5_*`, `y7_*`, `y9_*`
  (group-fold, vs size bar).
- Whether a no-invoice score cap is wanted for the demo.
- `score_new(csv_folder)` format.
