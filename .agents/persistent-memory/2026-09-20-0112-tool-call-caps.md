# Pregunta tool-call caps

- Author: agent
- Timestamp: 2026-09-20 01:12 +02:00
- Still-binding: No new tools. Same catalog. Cap 2 calls per tool per turn (1 for list/plot). Stop at 6 steps or 8 calls. Identical replay is memoized.

## What changed

The model was stacking `get_company` / `explain_change` / `get_alerts` / `get_control_chart`. `get_company` already returns reasons and change_reasons. Server now drops a tool after two uses and ignores an identical second call.
