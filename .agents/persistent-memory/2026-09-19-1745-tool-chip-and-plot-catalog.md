# 2026-09-19-1745 — Simpler tool row + catalog plots

- **Author:** agent (Cursor / Walter)
- **When:** 2026-09-19 17:45 CEST

## What changed

- Popup subtitle has no “con herramientas”.
- Tool row: wrench + Spanish label + seconds. The word `tools` is gone.
- `plot_series` is a closed catalog of eight charts (the ones on Resumen / Índice / Grupos / Javi’s monitor). The server fills the series. No typed values, no inflows.

## Decisions

- Improvisation allowed only as `control_own` with `score` / `payment_history` / `amounts_owed` / `stability`.
