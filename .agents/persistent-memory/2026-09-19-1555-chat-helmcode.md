# 2026-09-19 1555 — Chat uses Helmcode, not AI Gateway

Author: Ruben (Cursor)

## What changed

- `POST /api/chat` infers via Helmcode (`createHelmcodeModel`, same as Ask/Watcher): OpenAI-compatible `https://api.helmcode.com/v1`, `HELMCODE_API_KEY`, default model `deepseek-v4-flash`.
- UI stays shadcn (FAB + overlay). Helmcode is the inference API, not a chat widget.
- `.env.example` no longer documents AI Gateway for this chat; placeholder key only (never a real key).

## Decisions

- One LLM vendor for the demo: Helmcode (EU, flat-rate open models). Gateway dropped for the Health Score chat after 403 credit-card on the Vercel team.

## Still unknown

- Whether Ask tools stay Helmcode-only when the agent grows; assumed yes.
