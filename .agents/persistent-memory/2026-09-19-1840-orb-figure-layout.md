# Orb landing figure layout

- Author: Cursor Grok
- Timestamp: 2026-09-19 18:40 CEST

## What changed

`/orb` composition now follows the Speridlabs hero: left copy (eyebrow, title, body, two CTAs), right figure with corner crop marks, `Fig. 01.A` / `Índice 0–100` under the frame, faint reflection below. Nav is logo + text links + black Empresa.

The ASCII orb generator is unchanged. The sphere stays in the right column, centered in that figure, and does not overlap the title or subtitle.

## Still unknown

Whether this layout moves to `/` or stays an experiment.
