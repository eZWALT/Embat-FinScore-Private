# 2026-09-19-0525 — drop f_ds_r from the 15-col card

- **Author:** agent (Cursor)
- **When:** 2026-09-19 05:25 CEST

## What changed

[f_ds_r leftover QA](572fb928-40f3-4948-9ab9-0fde761a9d50):
**DROP** `f_ds_r` from the 15-col Y3 card and the 44 (parent decision;
do not rewrite `gbm_core.py` tonight). Unused leftover after days
rank **0.528** (OLS 0.709 is an almost-fake days leak, ρ(resid,days)
**0.775**). Inverse days-after-ds_r **0.682**. Single **0.620** vs
days 0.711 vs size 0.617 (Δ **0.003**, fails beat-size). Leftover
after fc **0.606** still lives; ρ vs fc **0.205**. Twin of euro
`a_debt_service` ρ **0.881**. i_lift drop-ds_r-only still **0.7525**.
Store flow stays. `f_fc_r_lag3` stays on TURNOVER. Javier
`debt_serv_r` SAME. Q6 **CLOSE**. Night quotes unchanged.

## Decisions

- Resume same owner for unused `c_salary_month` leftover after days
  (last unused card stem besides ss in-flight and the days bar).
  Cap 3.
- Still running: c_ss_month, issued.

## Still unknown

Whether `c_ss_month` leftover after days lives. Whether
`c_salary_month` leftover after days lives. Whether issued leftover
after days lives as Y3 X.
