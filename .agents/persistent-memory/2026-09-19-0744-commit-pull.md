# Commit + pull (ahead, not pushed)

- Author: parent
- Timestamp: 2026-09-19 07:44 CEST

## What changed

Second night commit after `e943582`. Saves leftover QAs and literature
waves written since that hash: `a_op_out` CLOSE, `a_debt_service` files,
Siddiqi `y3_reasons`, Norden `days_delta`, Hirshleifer `top1_pastdue`,
invoice/cash-flow literature notes, journals, LIVE/QUEUE/CONTEXT.

`git pull --rebase` onto `origin/main` (`4425998`). Do **not** push.

The 20-minute tick loop (`581046`, pid 114745) exited at the old 07:30
hard stop. Leftover deadline is **08:00**. Literature extras until
**~10:00**. Do not treat loop exit as kill-children.

## Decisions

- Commit the dirty leftover/literature tree. Exclude parquet, duckdb,
  `overnight/PYTHON`.
- Restart a 20-minute tick only until 08:00. Cap 3 leftover. No kills.
- Product stays frozen. No assembler. No `gbm_core` rewrite. No TURNOVER
  growth. No 15-col card edits.

## Still unknown

Whether `d_cust_lost` / `a_fin_cost` have an official parent absorb yet.
Literature Wave B leftovers (`issued_top1`, `runway_window`) still queued.
Hidden 72 stays unused for fit.
