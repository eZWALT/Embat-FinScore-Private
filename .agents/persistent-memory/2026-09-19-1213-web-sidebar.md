# 2026-09-19 12:13 — dashboard reorganized around shadcn Sidebar

- **Author:** Codex
- **When:** 2026-09-19 12:13 CEST

## What changed

- Replaced the horizontal product header with the official shadcn `Sidebar` in `product/web/`.
- Moved brand, company selector, section navigation, local-data status and light/dark choice into the sidebar.
- Added real anchors for Resumen, Evolución, Categorías and Señales; the mobile drawer closes after navigation or company selection.
- Added a compact sticky context header in the main area with the current company, group and reporting month.
- Replaced the generated `use-mobile` effect with `useSyncExternalStore` to satisfy React 19 lint rules and avoid a cascading render.

## Decisions

- Sidebar is 16rem, collapses to icons on desktop and becomes a shadcn Sheet below 768px.
- No placeholder product sections were added; navigation only points to content that exists today.
- Theme and data-source controls live in the sidebar footer.

## Verification

- `pnpm lint`, TypeScript, `pnpm build` and `git diff --check` pass.
- Browser verified: expanded and collapsed desktop sidebar, mobile Sheet, anchor navigation, company context and both charts.
- Browser console has no errors or warnings.

## Still unknown

- Active navigation is currently static on Resumen; scroll-spy can be added if the single page becomes longer.
