# 2026-09-19-0105 — Family J amount-match rates

- **Author:** agent `d56ee5fe` (Cursor)
- **When:** 2026-09-19 01:05 CEST

## What changed

Emitted Family J as company-month *rates* (not pairs). QA lane continues;
`data_join_qa.py` is read-only. No parquet merge, no FAMILIES, no Y8, no interco map.

## Files

- `analysis/features/match.py` — `FAMILY="j"`, `SOURCE_TABLES=["transactions","invoices"]`, `build(con, grid)`
- `analysis/outputs/match_report.md` — train-only coverage / size / acf / rewrite / Y
- `overnight/waves/wave4_data_qa.md` — appended Family J + `y_erp_gap_then_cash` label note
- `analysis/experiments/registry.csv` — append-only J coverage rows

## Decisions

- Emit `j_pay_match`, `j_iss_match`, `j_has_book`. Payment-month `|Δ|≤0.01`, greedy 1-1.
- 470: `j_pay_match` is NaN (not 0). `j_has_book` = 0.
- iss stays (ρ vs pay = 0.678 < 0.9). unmatched PARK (ρ = −1). t3 CLOSE (acf3 ≈ 0).
- Counts PARK: Y3 |ρ| ≈ 0.15 is size (`residual | log1p(a_in3)` ≈ 0).
- **J is not a Y3 X** (pay vs Y3 = −0.060). KEEP as Q5 diagnostic / miss flag.

## Still unknown

- Whether a parent merge of J into the store helps a Q5 LLM / explain path.
- `y_erp_gap_then_cash` power on the 470 stressed slice (design only; no module).
