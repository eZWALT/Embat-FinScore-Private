# 16h CLI search

- **Author:** agent (Cursor)
- **When:** 2026-09-18 ~21:40 CEST

## What changed

- `overnight/compare.py`: `evaluate_scores()` callable from a search loop; faster AUROC; lead-time uses per-company score lists.
- `overnight/search_16h.py`: wall-clock `--hours 16` loop. Tries score-card weights × Y cutoffs (`cash_q`, `ov_q`, `or`/`and`). Writes `runs/LIVE.json`, keeps `runs/deadline.txt` so a restart continues the same window. Saves a candidate when `primary ≥ 0.57` and gates pass.
- `overnight/watchdog.sh`: if `search_16h.py` dies before the deadline, start it again.

## Decisions

- Overnight work is a **detached Python process**, not the chat agent. Agent chat cannot be guaranteed for 16h.
- Y is still data-defined (own-history cash + overdue cuts), not the score itself.

## Still unknown

- Whether any combination clears `primary ≥ 0.57` on this holdout.
- First-pass feature cache time (full `transactions.csv` + `invoices.csv` in Python).
