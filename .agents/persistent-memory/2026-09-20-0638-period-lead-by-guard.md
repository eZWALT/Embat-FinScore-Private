# 2026-09-20 06:38 — Period lead groups by guard

Author: cursor-grok-4.6 (overnight Pregunta)
Timestamp: 2026-09-20 06:38 +02

## What changed

BREVITY + ROLE: the period first line groups by `guard` + real motive with «Empresa N». Template: tope / mora / estable. Never «las que caen» as one cause. Bench flags `tres empresas que caen… hundid`. Multi-company tool rows show one quote per empresa (so four companies fit in the four-quote cap).

## Decisions

- Keep vs main sample suite: 1/6, 1/6, 0/5, 4/6, 1/6. Period lead matched the template; text 1161 vs 1516.
- Production Pregunta (main) still asks for `COMP_xxxx` on unscoped «¿Qué alertas hay?». This branch does not.
- Do not merge to main tonight.

## Still unknown

The bench period question still embeds English `fading` (stale vs live `buildPrompt`, which uses `trajectoryLabel`). Align the harness next so it matches the drag copy.
