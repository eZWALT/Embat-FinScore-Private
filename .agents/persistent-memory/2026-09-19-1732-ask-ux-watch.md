# 2026-09-19-1732 — Ask UX watch + product copy

- **Author:** agent (Cursor / Walter)
- **When:** 2026-09-19 17:32 CEST

## What changed

- Popup subtitle no longer says Neon / core / `Sobre COMP_ · GROUP_`. Title is **Pregunta**; the line is what you can ask.
- Vigilancia empty state and Watcher reply copy in Spanish, product tone.
- Side script `product/web/scripts/watch-ask.mjs` (`pnpm watch:ask`): full Ask stack, tool loop, TTFT, time to last token, then a judge (`good` / `confusing` / `verbose` / `thin` / `off_scope`, 1–5). Writes `src/lib/agent/WATCH.md`. Judge prompt is `src/lib/agent/watch/judge.md` — not loaded in production.

## Decisions

- Stubs for tool bodies when `DATABASE_URL` is missing so we still measure the Helmcode loop.
- Do not shrink the Ask prompt for this watch.

## Still unknown

- Neon execute ms on the real Vercel path.
