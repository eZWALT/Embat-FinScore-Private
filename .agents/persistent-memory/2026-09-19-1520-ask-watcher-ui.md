# 2026-09-19 1520 — Ask chat + Watcher reply UI

- **Author:** Cursor Grok
- **When:** 2026-09-19 15:20 CEST

## What changed

- `/ask` is a real chat. The page loads company/group lists from `createScoreRepository()` (Neon). The client (`AskChat`) keeps the selects and the thread. Two English suggested questions per selected context (company vs group).
- `#sentinel` (`WatcherChannel`) keeps the three deterministic month posts. A **Reply** box under them posts to `/api/watcher/reply` and appends user/assistant bubbles. Opening posts are not regenerated.
- Shared client: `AgentChat` → `POST /api/ask` or `POST /api/watcher/reply` with `{ messages, companyId?, groupId?, asOf? }` over the AI SDK UI-message stream. Tool names render with existing `ToolCallChip` (wrench + `tools` + `(tool_name)`).
- Product links: `ProductNav` on a second `/grupos` header row (heatmap layout unchanged). Extra **Product** `SidebarGroup` on the company dashboard, separated from Ruben's hash sections.
- Watcher/Ask copy is English. Spanish company dashboard copy left as-is.

Did not touch `watcher-post.ts` or `prompts/watcher_format.md`. `tsc --noEmit` passed.

## Decisions

- Server-load options, client chat. Context change remounts the thread (`key` + `useChat` id).
- Suggested questions only when that context is selected (both selected → four chips).
- Live LLM is replies only. Month briefs stay the fixed `WatcherPost` renderer.

## Still unknown

- No `product/web/.env.local` here and no Next dev server was up, so the chat was not clicked against Neon/Helmcode.
- Live Ask/Watcher replies still need `DATABASE_URL` and `HELMCODE_API_KEY` (API returns 503 without them).
- Whether a severity filter or supplier twin of the top-customer alert belongs in the reply wording later.
